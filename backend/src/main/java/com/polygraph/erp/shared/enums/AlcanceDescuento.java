package com.polygraph.erp.shared.enums;

/** A qué aplica un descuento. GLOBAL no requiere referencia; CATEGORIA/PROCESO sí. */
public enum AlcanceDescuento {
    GLOBAL,
    CATEGORIA,
    PROCESO
}
